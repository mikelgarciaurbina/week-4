<h1>Search: <%= @search %></h1>

<ul>
  <% @contacts.each do |contact| %>
    <li>
    	<%= link_to contact.name, contact_path(contact) %>
    	<% if contact.favorite %>
			<img src="../favorite.png" alt="favorite" height="12">
    	<% end %>
    </li>
  <% end %>
</ul>